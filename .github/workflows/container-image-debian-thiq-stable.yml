name: WeeChat - Debian Thiq - Stable
on:
  push:
    branches:
      - main
  schedule:
    # 00:00 on Sunday
    - cron: "0 0 * * 0"
  workflow_dispatch: {}

concurrency:
  group: "weechat-stable-debian-thiq"
  cancel-in-progress: true

jobs:
  build-and-push:
    permissions:
      contents: write
      packages: write
    uses: ./.github/workflows/container-image-ci-base.yml
    with:
      base: debian
      flavor: thiq
      build-type: tag
